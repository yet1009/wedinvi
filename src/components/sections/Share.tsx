import classNames from 'classnames/bind';
import styles from './Map.module.scss';
import Section from '@shared/Section';

const cx = classNames.bind(styles);

const Share = () => {
  return <Section title={'공유하기'}></Section>;
};

export default Share;
